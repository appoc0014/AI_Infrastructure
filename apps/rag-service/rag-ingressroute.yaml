apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rag-service
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`rag.homelab.local`)
      kind: Rule
      services:
        - name: rag-service
          port: 8080

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: vllm-qwen
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`vllm.homelab.local`)
      kind: Rule
      services:
        - name: vllm-qwen
          port: 8000
